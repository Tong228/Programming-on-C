#include <stdio.h>
#include <math.h>

int main() {
    int a, b ,c, perimeter;
    printf("Введите три стороны треугольника: ");
    if (scanf("%d", &a, &b, &c)) 
    {

    }
    perimeter = a + b + c;
    

    
}